#pragma once

namespace EGE {

/// <summary>
/// The BMP file format, also known as bitmap image file or device independent bitmap (DIB) file format or simply a bitmap,
/// is a raster graphics image file format used to store bitmap digital images,
/// independently of the display device (such as a graphics adapter),
/// </summary>
class IBMPFile : public IImageFile {
public:
};

} // namespace EGE